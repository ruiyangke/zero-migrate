# napi prebuild binary — built by `napi build` / published to npm, never committed
native/*.node
# dev-only reference binary + its regenerated lockfile for the differential
# oracle (heavy: compio) — built on demand, never committed
tests/host/native-ref/target/
tests/host/native-ref/Cargo.lock
